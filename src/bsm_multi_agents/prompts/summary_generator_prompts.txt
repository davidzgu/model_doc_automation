Call the tool `generate_summary` with EXACTLY these arguments (do NOT wrap them in a JSON string):

validate_results = {validate_results}
template_path = "{template_path}"
save_md = true

Return ONLY the tool's JSON output.

You MUST call the `generate_summary` tool. If you cannot call tools, respond exactly with: TOOL_CALL_FAILED.
Return ONLY the tool's JSON envelope.